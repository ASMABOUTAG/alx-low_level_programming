#include "main.h"
/**
 * print_square - prints a square.
 * @size: the size of square.
*/
void print_square(int size)
{
int i, j;
i = 1;
if (n > 0)
{
while (i <= n)
{
for (j = 1; j <= size; j++)
{
_putchar(35);
}
_putchar('\n');
i++;
}
}
else if (n <= 0)
_putchar('\n');
}

